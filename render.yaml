services:
  - type: web
    name: sheetgenie
    runtime: node
    rootDir: nextjs
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
